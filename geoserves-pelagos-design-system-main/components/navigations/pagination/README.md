# Pagination

Page navigator for long lists and tables.

```jsx
import { Pagination } from '@pelagos/components/navigations/pagination';

<Pagination page={currentPage} totalPages={24} onChange={setCurrentPage} />
```

Live docs: `/pages/nav-pagination.html`
