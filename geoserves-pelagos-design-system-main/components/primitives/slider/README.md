# Slider

Range input for numeric value selection.

```jsx
import { Slider } from '@pelagos/components/primitives/slider';

<Slider label="Opacity" value={opacity} onChange={e => setOpacity(e.target.value)} min={0} max={100} />
```

Live docs: `/pages/prim-slider.html`
