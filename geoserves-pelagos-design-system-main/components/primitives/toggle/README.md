# Toggle (Switch)

On/off toggle for boolean settings.

```jsx
import { Toggle } from '@pelagos/components/primitives/toggle';

<Toggle label="Enable notifications" checked={enabled} onChange={e => setEnabled(e.target.checked)} />
```

Live docs: `/pages/prim-toggles.html`
